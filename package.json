{
  "private": true,
  "scripts": {
    "lint": "node --check content.js && node --check settings.js && node --check background.js && node --check options.js && python3 -m json.tool manifest.json >/dev/null && python3 -c \"from pathlib import Path; p = Path('native-host/save_file.py'); compile(p.read_text(encoding='utf-8'), str(p), 'exec')\" && bash -n install_host.sh && git diff --check && git diff --cached --check",
    "test": "node --test tests/*.test.js && python3 -m unittest tests/test_native_host.py",
    "check": "npm run lint && npm test"
  }
}
